{
  "dependencies": {
    "@observablehq/notebook-kit": "^2.0.0"
  },
  "scripts": {
    "preview": "notebooks preview --root nbks --template nbks/nochrome.tmpl",
    "build": "notebooks build --root nbks --template nbks/nochrome.tmpl --out ../dist -- nbks/*.html"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
